version: "3"

grpc:
  listen: tcp://0.0.0.0:9001
  proto:
    - "shared/grpc/cart.proto"

server:
  command: "php grpc-worker.php"
  relay: pipes

pool:
  num_workers: 4
  max_jobs: 0
  allocate_timeout: 60s
  destroy_timeout: 60s

logs:
  level: error
  encoding: console
  output: stderr