services:
    krakend:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '${APP_PORT:-80}:80'
        environment:
            APP_ENV: '${APP_ENV:-production}'
        networks:
            - app_network
networks:
    app_network:
        external: true
